{
    "name": "@askmi/consent-ui",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "default": "./dist/index.js"
        }
    },
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc -p tsconfig.build.json",
        "lint": "tsc --noEmit",
        "test": "vitest run",
        "typecheck": "tsc --noEmit",
        "clean": "rimraf dist"
    },
    "dependencies": {
        "@askmi/policy-engine": "workspace:*"
    },
    "devDependencies": {
        "jsdom": "^28.1.0",
        "rimraf": "^5.0.0",
        "typescript": "^5.3.0",
        "vitest": "^4.1.8"
    }
}
