{
    "name": "@mitch/policy-engine",
    "version": "0.0.1",
    "type": "module",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "default": "./dist/index.js"
        }
    },
    "files": [
        "dist"
    ],
    "scripts": {
        "clean": "rimraf dist",
        "build": "tsc -p tsconfig.build.json",
        "lint": "tsc --noEmit",
        "test": "vitest run"
    },
    "dependencies": {
        "@mitch/shared-types": "workspace:*",
        "@mitch/predicates": "workspace:*"
    },
    "devDependencies": {
        "typescript": "^5.0.0",
        "rimraf": "^5.0.0"
    }
}