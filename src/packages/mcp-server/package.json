{
  "name": "@mitch/mcp-server",
  "version": "0.0.1",
  "type": "module",
  "description": "MCP server — exposes the miTch policy engine to LLM agents via stdio",
  "bin": {
    "mitch-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc -p tsconfig.build.json",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@mitch/policy-engine": "workspace:*",
    "@mitch/shared-types": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.10.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.0"
  }
}
